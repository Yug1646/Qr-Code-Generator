info:
  name: Product QR Code
  type: http
  seq: 3

http:
  method: POST
  url: "{{base}}/qrCode/product-qr"
  body:
    type: json
    data: |-
      {
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
